#include "main.h"

/**
 * _strlen - count the length of a string
 *
 * @s: the string
 *
 * Return: the length os s
 */

int _strlen(char *s)
{
	int i;

	i = 0;
	while (*(s + i) != '\0')
		j++;
	return (j);
}
